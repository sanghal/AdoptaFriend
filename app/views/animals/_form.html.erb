


<%= simple_form_for(@animal) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

    <%= f.input :user_id %>

    <!-- < %= f.input :project_id, collection: Project.active.alphabetical, prompt: "Select project.." %> -->
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>



